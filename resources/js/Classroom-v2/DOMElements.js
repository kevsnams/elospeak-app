const container = document.getElementById('vr-board');
const drawingboard = document.getElementById('vr-drawingboard');
const curtain = document.getElementById('vr-curtain');

export default {container, drawingboard, curtain};